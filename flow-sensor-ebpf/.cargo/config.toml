[target.bpfel-unknown-none]
linker = "bpf-linker"
# Passed to LLVM during codegen (must also be set via RUSTFLAGS in build.sh if config is skipped).
rustflags = ["-C", "llvm-args=-bpf-stack-size=524288"]
